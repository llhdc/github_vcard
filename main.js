// TODO: use template literal to make main (with class of container); center h1; two h2 (class of header), two p tags (width: 35% + 65% respectively to start). p tags are flex items (display:flex on container); the story p tag has lightgrey border-left  
// TODO: examine github api data
// TODO: create AJAX request to pull info from personal profile and add to p tag


let vCard = document.querySelector('body');
